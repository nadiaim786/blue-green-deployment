apiVersion: v1
kind: Service
metadata:
  name: bg-service
  namespace: expt
spec:
  type: NodePort
  selector:
    app: bg-app
    version: blue
  ports:
    - port: 80
      targetPort: 80